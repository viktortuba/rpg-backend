# Generate a cryptographically strong JWT secret using Node.js:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# This creates a 512-bit (64-byte) random secret suitable for production use
JWT_SECRET=super-secret-key

DATABASE_URL=postgres://postgres:postgres@postgres:5432/game_db

REDIS_URL=redis://redis:6379
